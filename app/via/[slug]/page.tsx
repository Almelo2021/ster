import Home from '../../page'

// Attribution landing paths (/via/ze, /via/x123, ...) render the homepage;
// the path itself is captured verbatim by the logging middleware.
export default function Via() {
  return <Home />
}
